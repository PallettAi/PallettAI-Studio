# Only the webhook function is configured here. JWT is off because Stripe
# calls this URL; apply_stripe_entitlement stays service_role-only.
project_id = "fjahxichioccknszuxhb"

[functions.stripe-webhook]
verify_jwt = false

[functions.translate]
verify_jwt = true

[functions.billing-portal]
verify_jwt = true
